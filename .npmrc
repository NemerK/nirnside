; better-sqlite3 ships a Windows/macOS/Linux binary. npm still sees
; binding.gyp and tries to compile C++ (Visual Studio, Python, node-gyp).
; That compile is unnecessary and fails on most player PCs. Skip lifecycle
; scripts so the bundled binary is used as-is.
ignore-scripts=true
